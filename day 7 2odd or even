#include<stdio.h>
int main(){
	int num;
	printf("enter the number: ");
	scanf("%d",&num);
	if(num){
		if(num%2==0){
			printf("its an even number.");
		}else{
			printf("its an odd number.");
		}
	}else{
		printf("invalid number.");
	}
	
	
	return 0;
}
